#include <iostream.h>
#include <fstream.h>
#include <unistd.h>

//#include "dsm/DAVTraversable.H"

int main(int argc, char** argv)
{
/*
  const char *rootURL = "http://brig.emsl.pnl.gov:1080/webdav";
  TNode root;
  root.setURL(rootURL);
  root.setName(rootURL);
  //root.setLeaf(0);
  //DAVTraversable dt(rootURL);
  RWTPtrOrderedVector<TNode> *children = NULL;

 // children = dt.getChildren(&root);
*/
  return 0;
}
