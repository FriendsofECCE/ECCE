################################################################################
# FILENAME:  Makefile (generated by genmake)
#
#
# DESCRIPTION:
#   You should leave things in the order in which genmake wrote them.  Sections
#   where values can be modified are marked as MODIFIABLE.    
#
#   For an example of how to set up platform specific flags, see the bottom of
#   $ECCE_MAKEINC_DIR/Makefile.defs.
#
# CUSTOMIZATIONS:
################################################################################

# genmake:  include all ECCE definitions
include $(ECCE_MAKEINC_DIR)/Makefile.defs
# genmake:  includes for packages, dependencies, rules
include $(ECCE_MAKEINC_DIR)/Makefile.xerces
include $(ECCE_MAKEINC_DIR)/Makefile.wx

# genmake:  ALL MODIFIABLE
SRCS   = $(shell ls *.C 2>/dev/null)
OBJS   = $(patsubst %.C, $(OBJ_PATH)/%.o, $(SRCS))
#OBJS += $(OBJ_PATH)/util/*.o
IFLAGS := $(ECCE_IFLAGS) $(WX_IFLAGS) $(XERCES_IFLAGS)
CFLAGS := $(ECCE_CFLAGS)
LD      := $(CXX)
LFLAGS  := $(ECCE_LFLAGS) $(WX_LFLAGS) $(XERCES_LFLAGS)
LDFLAGS = $(ECCE_LDFLAGS) $(LFLAGS)

#########################################################################
# genmake:  libraries to link with
#
# The variables left undefined below need to be filled in by you.  This
# is because ECCE_LIBS (developed by us) and external packages have too
# many libraries that are possible to link with.  To see the available
# libraries for a particular package (eg OI_LIBS => NDOI) see the
# appropriate directory (eg for OI, look at $OIT_HOME/lib).
# 
# NOTE:  Values you place here need only be the "-lname" format.
#########################################################################
ECCE_LIBS = \
            -leccewxgui \
            -leccewxplotctrl \
            -leccewxthings \
            -lecceedsi \
            -leccedav \
            -leccecipc \
            -leccefaces \
            -leccexml \
            -leccetdat \
            -lecceutil

LIBS = $(ECCE_LIBS) $(WX_LIBS) $(XERCES_LIBS)

PROGRAMS = machregister
 
default: $(OBJ_PATH) $(SRCS:%.C=.%.D) $(PROGRAMS)

include $(shell ls .*.D 2>/dev/null)
include $(ECCE_MAKEINC_DIR)/Makefile.rules
